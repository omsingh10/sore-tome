import 'package:flutter/material.dart';

class TreasuryHome extends StatelessWidget {
  const TreasuryHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Treasury Dashboard')),
      body: const Center(child: Text('Treasury Controls')),
    );
  }
}






